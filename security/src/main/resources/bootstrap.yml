server:
  port: 2005

spring:
  application:
    name: security
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  redis:
    host: localhost
    port: 6379
    password:
    database: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:2001/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
